import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles((theme) => ({
  loader: {
    display: "flex",
    justifyContent: "center",
    marginTop: "4rem",
  },
  container: {
    marginTop: "0.8rem",
  },
  paper: {
    marginBottom: "1rem",
  },
}));
