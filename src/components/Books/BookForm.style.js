import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles(() => ({
  formContent:{
    textAlign: "center",
  },
  buttonControl: {
    width: "80%",
    height: "2.2rem",
    textTransform: "none",
    borderRadius: "2px",
  },
}));
